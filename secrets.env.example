# JobMail Secrets Template
# This file should NEVER be committed to git
# Copy this file to secrets.env and fill in your actual secrets

# IMPORTANT: secrets.env must be in .gitignore

# AI Provider API Keys
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...

# Gmail OAuth2 Credentials
# Download credentials.json from Google Cloud Console
# Place it in the project root (it will be referenced by GMAIL_CREDENTIALS_FILE)
# After first authentication, token.json will be created automatically

# Other configuration values can be set in secrets.env or use defaults from .env.example
